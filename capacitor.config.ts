import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'cs.cap6.test',
  appName: 'cs-cap6-test',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
